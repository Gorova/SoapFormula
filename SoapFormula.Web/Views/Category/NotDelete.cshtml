@model SoapFormula.DAL.Entities.Category
    ViewBag.Title = "NotDelete";
}

<h2>deleting category @Model.Name is impossible until it has products</h2>
<ul>
    @foreach (var product in Model.Products)
    {
        <li>@product.Name</li>
    }
</ul>
@Html.ActionLink("back to main page", "Index")




