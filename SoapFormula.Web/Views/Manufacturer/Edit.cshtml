@model SoapFormula.Web.ViewModel.ManufacturerViewModel
@{
    ViewBag.Title = "Edit";
}

<h2>Edit Manufacturer</h2>
<table>
    <tr>
        <td>
            @using (Html.BeginForm())
            {
                @Html.EditorForModel("ManufacturerEditor")
                <input type="submit" value="save" />
            }
        </td>
    </tr>
    <tr>
        <td>
            @Html.ActionLink("back to main page", "Index")
        </td>
    </tr>
</table>
