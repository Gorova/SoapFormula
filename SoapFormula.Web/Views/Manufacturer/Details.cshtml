@model SoapFormula.Web.ViewModel.ManufacturerViewModel
@{
    ViewBag.Title = "Details";
}

<h2>Details Manufacturer</h2>

<table>
    <tr>
        <td>
            @Html.LabelFor(i => i.Name)<br/>
            @Html.DisplayFor(i => i.Name)<br/>
            @Html.LabelFor(i => i.Products)
            <ul>
                @foreach (var c in Model.Products)
                {
                    <li> @c.Name</li>
                }
            </ul>
        </td>
        </tr>
    <tr>
        <td>
            @Html.ActionLink("back to main page", "Index")
        </td>
    </tr>


</table>
