@model SoapFormula.Web.ViewModel.ManufacturerViewModel
@{
    ViewBag.Title = "Delete";
}

<h2>@ViewBag.Title</h2>

<h3>Are you sure you want to delete manufacturer</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(i => i.Name)
        </dt>

        <dd>
            @Html.DisplayFor(i => i.Name)
        </dd>
    </dl>

    @using (Html.BeginForm())
    {
        <div class="form-actions no-color">
            @Html.HiddenFor(i => i.Id)
            <input type="submit" value="delete" class="btn btn-default" />
            <input type="button" value="cancel" class="btn btn-default" onclick="location.href = '@Url.Action("Index", "Manufacturer")'" />
        </div>
    }
</div>



      
   
            