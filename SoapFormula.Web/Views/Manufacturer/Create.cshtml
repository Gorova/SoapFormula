@model SoapFormula.Web.ViewModel.ManufacturerViewModel
@{
    ViewBag.Title = "Create";
}

<h2>Create Manufacturer</h2>

<table>
    <tr>
        <th>
            write a manufacturer
        </th> <th></th>
    </tr>
    <tr>
        <td>
        @using (Html.BeginForm())
        {
            @Html.HiddenFor(i => i.Id)
            @Html.TextBoxFor(i => i.Name)
            <p> choose product</p>
            @*@Html.HiddenFor(i => i.Id)
            @Html.DropDownListFor( i => i.Products, Model.Products)<br />*@
            <input type="submit" value="create"/>
        }
        </td>
    </tr>
    <tr>
        <td>
            @Html.ActionLink("back to main page","Index")
        </td>
    </tr>
</table>